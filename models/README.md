# Models

This folder is intended to store trained machine learning models.

The trained model files are not included in this repository because they were generated using confidential IBM SkillsBuild learner data.

To reproduce the models, run the training pipeline available in the `src/` directory using your own dataset.
